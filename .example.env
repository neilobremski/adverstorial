# Adverstorial Environment Variables
# Copy to .env and update the values accordingly
# NOTE: A .env file is only loaded if python-dotenv is installed

PYTHONLOGGING=INFO

PAYI_API_URL=https://api.pay-i.com/
PAYI_PROXY_DIRECT=false
PAYI_PROXY_INGEST=false
PAYI_PROXY_URL=https://developer.pay-i.com/proxy/
PAYI_VERIFY_SSL=true

MAX_OUTPUT_TOKENS=3000
REASONING_EFFORT=minimal
ROUNDS=2
TEMPERATURE=0.5,2.0

# Lists are used for random selection; PROTAGONISTS/ANTAGONISTS take precedence over ADVERSARIES when set.
ADVERSARIES=openai.gpt-5,anthropic.claude-sonnet-4-5,anthropic.claude-opus-4-1,openai.gpt-4.1-mini,openai.gpt-4o-mini,openai.gpt-3.5-turbo
# PROTAGONISTS=
# ANTAGONISTS=
# Fixed overrides are also possible and are useful for testing specific model matchups.
# PROTAGONIST=openai.gpt-4.1-mini
# ANTAGONIST=anthropic.claude-sonnet-4-5

# NOTE: Azure OpenAI Adversaries look like this: azure.openai.<model>/<price-as>
# The <model> is your Azure deployed model and <price-as> is its base in OpenAI.
# Ex: "azure.openai.supercool/gpt-5" == "supercool" is a deployment of "gpt-5"

# Optional
# DEFAULT_ACCOUNT_NAME=""
# DEFAULT_USER_ID=""

ANTHROPIC_API_KEY="<your-anthropic-api-key-here>"
OPENAI_API_KEY="<your-openai-api-key-here>"
AZURE_OPENAI_API_KEY="<your-azure-api-key-here>"
AZURE_OPENAI_BASE_URI="https://<uri>.azure.com/"

# Proxy shadow to Azure Blob Storage (optional)
# NOTE: BLOB_STORAGE_TOKEN="auto" means use az CLI to obtain bearer token
# BLOB_STORAGE_PATH=""
# BLOB_STORAGE_TOKEN="auto"
